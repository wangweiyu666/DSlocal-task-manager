{
  "name": "dstationery-cloud",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "engines": { "node": ">=22" },
  "scripts": {
    "dev": "wrangler dev --local",
    "typecheck": "tsc --noEmit",
    "test": "vitest run",
    "check": "npm run typecheck && npm test",
    "smoke:local": "node scripts/smoke-local.mjs",
    "migrate:local": "wrangler d1 migrations apply dstationery-local --local",
    "deploy:staging": "node scripts/guard-environment.mjs staging && wrangler deploy --env staging",
    "deploy:production": "node scripts/guard-environment.mjs production && wrangler deploy --env production"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "5.20260816.1",
    "@types/node": "22.10.2",
    "typescript": "5.9.2",
    "vitest": "3.2.7",
    "wrangler": "4.123.0"
  }
}
